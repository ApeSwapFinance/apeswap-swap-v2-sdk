export enum Network {
  MAINNET = 1,
  // ROPSTEN = 3,
  // RINKEBY = 4,
  // GOERLI = 5,
  // GÖRLI = 5,
  // KOVAN = 42,
  // POLYGON = 137,
  // ARBITRUM = 42161,

  // ApeSwap Additions
  BSC = 56,
  BSC_DUMMY = -56, // Flipping the value to support two vaults on the same network
  BSC_TESTNET = 97,
}